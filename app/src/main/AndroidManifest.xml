<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.moodic">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moodic"
        tools:targetApi="31">

        <!-- Login Activity - LAUNCHER -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.SignUpActivity"
            android:exported="false" />

        <!-- Main Activity - Navigation Hub -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:launchMode="singleTop" />

        <!-- Mood Input Activity -->
        <activity
            android:name=".activities.MoodInputActivity"
            android:exported="false" />

        <!-- YouTube Search Activity -->
        <activity
            android:name=".activities.YouTubeSearchActivity"
            android:exported="false" />

        <!-- Results Activity -->
        <activity
            android:name=".activities.ResultsActivity"
            android:exported="false" />

    </application>

</manifest>